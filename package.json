{
  "name": "wasm-example",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@assemblyscript/loader": "^0.19.22",
    "@testing-library/jest-dom": "^5.16.1",
    "@testing-library/react": "^12.1.2",
    "@testing-library/user-event": "^13.5.0",
    "as-bind": "^0.8.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "5.0.0",
    "web-vitals": "^2.1.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build:react": "react-scripts build",
    "build:wasm": "asc assembly/index.ts --binaryFile public/wasm-example.wasm --optimize",
    "buildx": "npm run build:wasm && npm run build:react"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "assemblyscript": "^0.19.22"
  }
}
